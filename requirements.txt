# ============================================================
# realestate-channel — Python dependencies
# ============================================================
# Install: uv pip install -r requirements.txt
# ============================================================

# Core
python-dotenv>=1.0.0        # loads .env into environment
requests>=2.31.0            # HTTP calls to all APIs (YouTube, ElevenLabs, HeyGen, Postiz)

# UI
rich>=13.0.0                # terminal output formatting

# Content
youtube-transcript-api>=0.6.0   # fetch transcripts from YouTube videos (repurpose.py)

# Dev utilities
python-dateutil>=2.8.0      # date parsing for scheduling

# Events — Kafka is an alternate `events` provider (EVENTS_PROVIDER=kafka);
# console is the default and needs no extra dependency.
# Capped below 3.0: that release rewrote kafka-python's networking/protocol
# layer (new constructor surface), while kafka_events.py targets the 2.x
# KafkaProducer API. Broker compatibility is unaffected — Kafka is backwards
# compatible, so the pinned 2.x client still works fine against Kafka 4.x.
kafka-python>=2.0.2,<3.0.0

# ============================================================
# Future / optional — uncomment when needed
# ============================================================

# Pipeline B — only if switching from REST to SDK
# elevenlabs>=1.0.0          # ElevenLabs SDK (currently using REST API directly)

# Analytics — only if switching from REST to Google client library
# google-api-python-client>=2.100.0
# google-auth>=2.23.0
# google-auth-oauthlib>=1.1.0

# Async HTTP — only if Postiz calls need async
# httpx>=0.25.0